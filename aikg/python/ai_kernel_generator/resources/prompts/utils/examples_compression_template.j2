# {{ dsl }} 示例代码智能压缩与筛选

你是一个专业的AI领域 Kernel编写专家Agent，熟悉各种硬件后端、各种DSL的Kernel编写。
当前的任务是需要你从大量的{{ dsl }}示例代码中提取最有价值的部分，去除重复和冗余内容，生成精简而实用的示例代码集合。

## 压缩原则
1. **去重原则**：删除功能相同或相似的示例代码，只保留最典型的一个
2. **价值优先**：优先保留包含核心概念、最佳实践、性能优化技巧的示例
3. **结构优化**：将相似的代码模式合并，提取通用模板
4. **长度控制**：确保最终输出不超过3000字符，保持简洁明了
5. **实用性**：优先保留可以直接参考使用的完整示例

## 筛选标准
- **高价值**：包含核心API使用、性能优化、错误处理等
- **完整性**：代码结构完整，可以直接运行或参考
- **代表性**：能够代表一类常见的使用模式
- **新颖性**：包含独特的使用技巧或最佳实践

## 当前算子任务描述
{{ task_desc }}

{% if sketch %}
## 当前算子Sketch设计
{{sketch}}
{% endif %}

## 框架信息
- DSL类型：{{ dsl }}
- 目标框架：{{ framework }}
- 算子名称：{{ op_name }}

## 原始示例代码
{{ dsl_examples }}

## 输出要求
请按照以下格式输出压缩后的示例代码：

1. **核心概念示例**：展示{{ dsl }}的基本使用模式和核心概念
2. **性能优化示例**：包含性能优化技巧的示例
3. **最佳实践示例**：展示推荐的使用方式和错误处理
4. **高级特性示例**：展示高级功能和复杂场景的处理

每个示例应包含：
- 简洁的注释说明
- 完整的代码结构
- 关键点说明

**请尽可能使用中文进行思考分析**

**请直接输出压缩后的示例代码，不要包含任何解释或额外内容：** 