你是一个Torch库、Numpy库、MindSpore库的解释专家。现在需要根据以下要求解析代码中的函数操作，并返回相关信息。

【要求】请简要分析，思考过程不得过于冗长

0. 这是需要解析的代码：
{{ task_code }}

1. 这是需要参考返回内容的要求，下面是核心解析规则：
1.1 算子类型识别 (op_type)：
    Elementwise：逐元素操作，无维度变化 (如 torch.tanh(), x + y)
    Reduce：根据维度缩减的操作，进一步分类：
     - reduce_all：缩减所有维度
     - reduce_x：包含最后维度（水平方向）
     - reduce_y：包含倒数第二维度（垂直方向）
     - multi_reduce_axis：多轴缩减
    Broadcast：输入输出形状不同，无维度缩减

1.2. 操作名称解析：
    主操作名 (op_name_primary)：核心功能名 (如 "softmax")
    次操作名 (op_name_secondary)：实现主操作的底层函数列表 (如 ["exp", "sum"])

1.3. 数据量特征 (op_shape)：
    Elementwise/Broadcast：请自行根据数据量判断是small,middle还是big
    Reduce：分别针对reduce轴和非reduce轴，比较他们的数据量是大还是小，并分别描述轴的大小，描述信息要标记出reduce轴与非reduce轴。
1.4. 新增描述字段 (description)：
    请简单扼要地说明算子类型、适用场景、数据形状特征，不要展开其它详细的的叙述，例如：reduce和elementwise融合的softmax算子，适用于计算密集型场景，reduce轴小（64），非reduce轴大（1024）。

【注意事项1】尽量在保证正确性的情况下，降低思考过程，不要进行不必要的思考。
【注意事项2】请尽可能使用中文进行思考分析
    
**请按照以下格式输出你的结果，仅返回json字符串的格式，不要包含任何解释或额外内容：**

{{ format_instructions }}