- 从指定 URL 获取内容
- 接收 URL 和可选格式作为输入
- 获取 URL 内容，并转换为请求的格式（默认 markdown）
- 以指定格式返回内容
- 当需要检索并分析网页内容时使用此工具

使用说明：
  - 重要：如果存在更适合的工具（更定向、更少限制或更强大），优先使用该工具而不是此工具。
  - URL 必须是完整有效的 URL
  - HTTP URL 会自动升级为 HTTPS
  - 格式选项："markdown"（默认）、"text" 或 "html"
  - 此工具为只读，不会修改任何文件
  - 如果内容非常大，结果可能会被摘要
