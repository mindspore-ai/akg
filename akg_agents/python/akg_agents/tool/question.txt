当你需要在执行过程中向用户提问时使用此工具。它可以让你：
1. 收集用户偏好或需求
2. 澄清含糊的指令
3. 在实施过程中获取关键决策
4. 向用户提供可选方向

使用说明：
- 当 `custom` 启用时（默认），会自动添加一个 “Type your own answer” 选项；不要再包含 “Other” 或笼统选项
- 答案以标签数组形式返回；设置 `multiple: true` 可允许选择多个
- 如果你推荐某个选项，把它放在列表第一位，并在标签末尾加上 “(Recommended)”
