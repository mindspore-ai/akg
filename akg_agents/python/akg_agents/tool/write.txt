将文件写入本地文件系统。

用法：
- 此工具会覆盖提供路径上的现有文件（如果存在）。
- 如果是已有文件，必须先使用 Read 工具读取其内容。若未先读取，此工具会失败。
- 始终优先编辑代码库中已存在的文件。除非明确要求，否则不要写新文件。
- 绝不要主动创建文档文件（*.md）或 README 文件。只有在用户明确要求时才创建文档文件。
- 仅在用户明确要求时才使用表情符号。除非被要求，避免向文件中添加表情符号。
