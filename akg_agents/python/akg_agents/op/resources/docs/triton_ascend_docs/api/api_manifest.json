{
  "version": 1,
  "source_name": "api.md",
  "preamble": "# Triton Ascend Core API 参考手册\n\n本文档是 Coder 默认展示的 Triton Ascend core API 文档。这里列出的 API 会被视为“已展示 API”；Triton API database 召回到同名 API 时只补充候选关系，不重复展开详细文档。低频 API 不在本文档常驻展示，应通过 database recall 按任务补充。\n\n## 0. 高频 API 使用约束\n\n- `tl.arange(0, BLOCK_*)` 的逻辑长度应使用编译期常量，通常取 2 的幂；真实尾部用 mask 处理，不要把动态 runtime shape 直接作为 `end`。\n- `tl.load` / `tl.store` 的 pointer、mask、value shape 必须可广播到同一 block shape；mask 必须在内存访问前排除越界地址。\n- Ascend grid 必须是 tuple 且最多 3 维；当前 910B 运行环境默认开启 `TRITON_ALL_BLOCKS_PARALLEL=1`，允许超大一维逻辑 grid 由后端映射到物理核心循环。大 shape 优先使用 1D flattened grid 或固定核心数 grid-stride；不要为了压低 grid 数而放大每个 program 的 tile。\n- `tl.dot` 适合矩阵/批矩阵 contraction，输入通常为 `(BLOCK_M, BLOCK_K)` 和 `(BLOCK_K, BLOCK_N)`；不要用 `A * B` 冒充矩阵乘。\n- Ascend `tl.dot` 不要传 `allow_tf32` 或 `input_precision`；这些是 CUDA 精度控制语义，Ascend 后端不支持。\n- `tl.zeros`、`tl.full`、`tl.reshape`、`tl.static_range` 等涉及 shape 或循环边界的参数必须是编译期可确定的 literal 或 `tl.constexpr` meta 参数。\n- 动态循环优先使用 `tl.range`；只有小的编译期常量循环才使用 `tl.static_range`，不要在 kernel 中用依赖 runtime 参数的 Python `range(start, end, step)`。\n- 不要生成或调优 CUDA-only launch 参数，例如 `num_warps`、`num_ctas`、`num_stages`、`num_buffers_warp_spec`、`num_consumer_groups`、`reg_dec_producer`、`reg_inc_consumer`、`maxnreg`。\n- Triton language 中不要臆造 API；常见不存在的写法包括 `tl.div`、`tl.mod`、`tl.prod`。整数除法/取模优先用 `//`、`%`，乘积在 host 侧或静态表达式中展开。\n\n",
  "sections": [
    {
      "prefix": "## 1. 内核装饰器\n\n",
      "entries": [
        "triton.jit.md"
      ]
    },
    {
      "prefix": "## 2. 程序 ID 与网格 API\n\n",
      "entries": [
        "triton.language.program_id.md",
        "triton.language.num_programs.md",
        "triton.cdiv.md"
      ]
    },
    {
      "prefix": "## 3. 内存操作 API\n\n",
      "entries": [
        "triton.language.load.md",
        "triton.language.store.md",
        "triton.language.make_block_ptr.md",
        "triton.language.advance.md"
      ]
    },
    {
      "prefix": "## 4. 张量创建与操作 API\n\n",
      "entries": [
        "triton.language.arange.md",
        "triton.language.zeros.md",
        "triton.language.full.md",
        "triton.language.reshape.md",
        "triton.language.expand_dims.md",
        "triton.language.cast.md"
      ]
    },
    {
      "prefix": "## 5. 数学运算 API\n\n",
      "entries": [
        "triton.language.cdiv.md",
        "triton.language.dot.md",
        "triton.language.sum.md",
        "triton.language.max.md",
        "triton.language.min.md",
        "triton.language.maximum.md",
        "triton.language.minimum.md",
        "triton.language.where.md"
      ]
    },
    {
      "prefix": "## 6. 原子操作 API\n\n",
      "entries": [
        "triton.language.atomic_add.md",
        "triton.language.atomic_max.md"
      ]
    },
    {
      "prefix": "## 7. 编译期与循环控制 API\n\n",
      "entries": [
        "triton.language.constexpr.md",
        "triton.language.static_range.md",
        "triton.language.range.md"
      ]
    },
    {
      "prefix": "## 8. 块分配优化 API\n\n",
      "entries": [
        "triton.language.swizzle2d.md"
      ]
    }
  ]
}
