{% if skill_contents %}
## DSL 编程约束与调试知识

{{ skill_contents }}

---
{% endif %}

## 当前任务运行情况

算子名称: {{ op_name }}
前端框架: {{ framework }}

目标 Module 描述:
{{ task_desc }}

### 当前 Agent 返回结果
{{ agent_result }}

{% if error_log %}
### 错误日志
{{ error_log }}

**错误分类与处理指南：**

请首先判断错误类型，不同类型的处理方式不同：

#### 0. LLM 输出异常（非代码逻辑问题）

**0a. 输出被截断（max_tokens 不足）**
- 错误日志中包含 `finish_reason=length` 或 `truncated=True`
- 错误日志中包含 "KernelGen 输出异常" 或 "Coder 输出异常"
- 错误日志中提到 "LLM 输出被截断" 或 "content 可能不完整"
→ **这是LLM输出超出最大Token限制导致的，不是代码逻辑问题**
→ **应选择代码生成 Agent 重新生成**，建议中必须明确指出：
    - **控制显式推理预算**，如减少解释性文字，减少'让我想想''首先'等过渡语等；
    - 或**缩短代码长度**，如禁用冗余注释等；
    - **确保输出完整代码/JSON**
→ **不要建议"生成完整代码"或"修复语法错误"，这些建议治标不治本**

**0b. 输出被内容过滤器拦截**
- 错误日志中包含 `finish_reason=content_filter` 或 `content_filtered=True`
→ **这是模型安全策略拦截导致的，不是代码逻辑问题**
→ **应选择代码生成 Agent 重新生成**，建议中指出：避免触发安全过滤的词汇，使用更中性的技术术语

#### A. 代码逻辑/算法错误（可以通过修复代码解决）

请根据错误的**影响范围**选择修复方式：

**A1. 局部、可精确定位的错误** {% if 'fix_code_gen' in valid_next_agents %}→ 优先选择 `fix_code_gen`（增量修复）{% endif %}
- 缺少 import 语句（如 `NameError: name 'torch' is not defined`）
- 某个 API 调用不支持或参数错误（如 `tl.tanh` 不存在，需换成近似实现）
- 个别变量名拼写错误、类型不匹配
- 少量语法错误（缺少冒号、括号不匹配等）
{% if 'fix_code_gen' in valid_next_agents %}→ 这类错误只需修改 1-3 处代码，大部分代码是正确的，增量修复效率更高{% endif %}

**A2. 全局性、架构级的错误** → 选择代码生成 Agent 完全重新生成
- 整体算法思路不对（如使用了错误的并行策略）
- 数值精度问题涉及大量计算逻辑（需要重写核心算法）
- 内存布局、张量维度全面错误（需要重新设计数据流）
- Kernel 参数配置（BLOCK_SIZE、grid 等）需要全面调整
- 违反 DSL 编程约束（参考上方"DSL 编程约束与调试知识"中的禁止语法和常见错误表）
→ 这类错误需要重写大量代码，结合上方知识文档给出具体的修复建议，完全重新生成更合适

#### B. 环境/基础设施错误（代码生成 Agent 无法修复）
- 文件路径错误（FileNotFoundError、read_json_file 失败）
- 编码错误（UnicodeDecodeError、UTF-8 相关）
- 设备不可用（CUDA out of memory、device not found）
- 依赖缺失（ModuleNotFoundError、ImportError 非代码导致的）
- 配置错误（参数传递问题、状态丢失）
→ **应选择 finish**，因为这些不是代码问题，重新生成代码无法解决

#### B2. 超时/性能问题（可以通过优化代码修复）
- 超时（Timeout、进程被杀死、执行时间过长）
→ **这通常是代码性能问题**（死循环、算法复杂度过高、内存访问模式低效等）
→ **应选择代码生成 Agent 重新生成**，建议中指出优化方向（减少循环、优化内存访问、调整 tile 大小等）

如果错误属于 A 类，请结合上方"DSL 编程约束与调试知识"中的内容进行深入分析，定位具体的违规项或约束冲突。
{% endif %}

{% if history_attempts %}
### 历史尝试记录（最近{{ history_attempts|length }}次）
以下是过去的尝试和建议，请避免重复犯相同的错误：

{% for attempt in history_attempts %}
#### 尝试 #{{ loop.index }}
{% if attempt.suggestion %}
**当时的分析建议：**
{{ attempt.suggestion }}
{% endif %}
---
{% endfor %}
{% endif %}


## 当前的任务是
1. **首先判断错误类型**：结合上方 DSL 编程知识，区分代码问题（0/A/B2类）和环境问题（B类）；
2. 根据错误类型给出下一步的该交给哪个 Agent 继续运行；
3. 当有报错信息时，总结报错信息，使用不超过500字符长度的语言详细描述遇到的报错信息（摘取关键的原始报错信息）
4. 如果选择继续生成代码，参考上方的 DSL 约束和调试知识，为下个 Agent 提供精确、合理的修复提示；
5. **如果选择 finish**，在建议中说明终止原因。

## 输出
1. 下一步 Agent 名
2. 当前的报错信息与总结 + 针对当前错误的修复建议 + 综合过往的报错信息提醒不要犯错

## 下一步可选 Agents（必须从这里选择）
{{ valid_next_agents }}

**请按照以下格式输出分析结果，仅返回JSON，不要包含任何解释或额外内容：**

{{ format_instructions }}