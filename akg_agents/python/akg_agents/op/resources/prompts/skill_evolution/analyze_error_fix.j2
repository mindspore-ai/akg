你是一位内核调试专家。请从以下修复案例中提炼常见错误及修复方法。

## 算子信息
- 名称: {{ op_name }}
- DSL: {{ dsl }} | 后端: {{ backend }} | 架构: {{ arch }}

## 修复案例

{{ fix_cases }}

## 输出要求

直接输出 Markdown 正文（**不要**输出 skill_name、description 或 YAML frontmatter）。

**风格示例**（请严格参考此格式，只写报错特征和修复方法，不写原因分析）：

```
### UB 超限

- **报错特征**: `ub overflow`
- **修复**:
  - 根据硬件 UB 容量和算子代码计算合适的 BLOCK_SIZE
  - 减少中间 buffer 数量或大小

```python
# 错误：BLOCK_SIZE 过大导致 UB 超限
# 需结合硬件 UB 大小和算子代码计算

# 正确：根据硬件约束调整
# Ascend910B4 UB 192KB，需确保 total_ub_usage < UB_limit
```

### 形状不匹配

- **报错特征**: `incompatible dimensions`
- **修复**:
  - 检查 `tl.make_block_ptr` 的 offsets 与 block_shape 对齐
  - 使用标准 2D 网格替代复杂线程映射
```

### 关键原则

1. 每个错误用**简短标题**，只写**报错特征**和**修复方法**两项，不写原因
2. 报错特征精确到错误信息关键词
3. 修复方法具体到代码层面，附简短的错误/正确代码对比
4. 合并同类项：相似错误归为一个类别
5. 不要包含特定算子的实现细节，集合硬件信息聚焦可迁移的通用修复方法
