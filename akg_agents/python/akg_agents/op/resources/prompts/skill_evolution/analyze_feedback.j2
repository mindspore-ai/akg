你是一个高级算子优化专家。请根据以下**完整的对话 action 历史**，提炼可复用的人工调优经验，撰写一份 **SKILL.md** 优化经验文档。

这份文档的目标读者是**后续要实现类似算子的 AI agent**，它需要从中学习人类专家的优化思路和调优策略。

## 算子信息
- 名称: {{ op_name }}
- DSL: {{ dsl }} | 后端: {{ backend }} | 架构: {{ arch }}

## 完整对话 Action 时间线

以下是本次对话中所有工具调用的时间线记录。请你自行分析：
- 用户在哪些轮次给出了**实质性的优化建议**（不是"确认"、"ok"等确认消息）
- 哪些 `profile_kernel` 结果对应哪个版本的代码
- 用户建议带来了怎样的代码变更和性能变化
- 哪些优化尝试是有效的，哪些是无效的

{{ timeline }}

---

## 输出要求

请直接用 Markdown 撰写 SKILL.md 的**正文**（不需要写 YAML frontmatter，代码会自动补充）。

**核心目标：从对话历史中识别"用户建议 → 代码变更 → 性能变化"的因果链，提炼可迁移的人工调优经验。**

分析原则：
- **自行判断**哪些轮次包含有价值的优化经验，不依赖预定义的规则
- **自行匹配**代码版本和性能数据——profile_kernel 的结果对应它之前最近一次生成的代码
- **忽略**纯确认消息（"确认"/"ok"等）和无关操作（read_file、execute_script 报错等）
- 如果性能提升很小但用户的优化思路有价值（如调优策略、参数选择思路），仍然值得记录

禁止事项：
- **禁止**补充教科书式的通用知识（如基本的向量化、分块、mask 等）
- **禁止**编造因果关系——只描述有数据支撑的"建议→变更→效果"关联

文档结构要求：

1. **任务特征**（2-3 行） — 界定该经验所适用的问题类别（如"大序列逐元素操作"、"内存带宽受限算子"），不要罗列具体 shape。

2. **调优经验**（文档主体） — 每条经验格式：
   - 标题：经验名称（如"Block Size 调优策略"、"Warp 并行度配置"）
   - 场景：什么条件下应考虑此调优方向（1-2 句话）
   - 做法：用户建议的核心内容，泛化为可复用的方法论（2-3 句话）
   - 效果：性能变化和原因分析（1-2 句话）
   - 如有必要附一个最小代码片段（≤10 行）展示关键写法

3. **适用边界**（2-3 行） — 这些经验在什么条件下可复用，什么条件下可能不适用。

文档的前两行必须是（注意不要加任何代码围栏）:
skill_name: 命名格式为 `{dsl}-exp-{算子类别}-{调优特征}`，例如 `triton-cuda-exp-elemwise-blocksize-tuning`。当前 dsl={{ dsl }}。
description: 一句话描述此 skill 提炼的**人工调优经验**及其适用的问题类别（50-100 字），不要出现具体算子名或具体 shape。

后续跟一个空行再开始正文。

写作要求：
- 用中文撰写
- 聚焦泛化性：写出的经验应能指导同类新算子的调优，避免仅对当前算子有效的细节
- 宁少勿滥：如果只有 1-2 条经验，就只写 1-2 条，不要凑数
- 代码片段用 ```python 或 ```diff 格式
- 控制总长度在 40-150 行之间
