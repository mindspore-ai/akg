你是一位 Triton Ascend 内核调试经验整理专家。以下是一份包含多种错误修复记录的单一文档，请将其拆分为 2-4 个主题聚焦的修复指南。

## 原始文档

{{ fix_body }}

{% if existing_summaries %}
## 已有的 fix skill（不要重复）

以下 skill 已经存在，如果原始文档中的某些条目与它们**语义重复**，直接丢弃，不要输出：

{{ existing_summaries }}
{% endif %}

## 拆分规则

1. **按错误根因分组**：相同硬件限制（如 CBUF/UB 容量）、相同编码习惯（如语法/拼写）、相同编译阶段（如 BiShengHIR）的错误归为一组
2. **粒度适中**：2-4 个分组，不要每条单独成组，也不要全部塞进一组
3. **去冗**：重复或过于琐碎的条目（如"使用 torch.nn.Parameter 替代 nn.Parameter"这类代码风格问题）直接丢弃
4. **每组聚焦一个调试方向**：使 LLM 在 debug 阶段可通过 description 快速匹配到该组
5. **严格使用原文中的代码示例**，不要编造新的代码

## 每组输出要求

对每个分组，输出以下内容：

- **skill_name**：`{{ dsl_prefix }}-fix-{特征}`，英文短横线连接，2-3 个单词概括（如 `triton-ascend-fix-cbuf-overflow`）
- **description**：中文，50-80 字，明确列出本组覆盖的错误类型。格式参考："Ascend CBUF/UB 溢出类错误的诊断与修复方法，包括分块参数调优和中间变量拆分"
- **content**：每种错误按以下格式编写：

```
### 错误标题

- **报错特征**: `关键报错信息`
- **根因**: 一句话说明原因
- **修复**: 具体修复步骤

​```python
# 错误写法
...

# 正确写法
...
​```
```

- content 末尾附上 Quick Checklist：

```
---
## Quick Checklist

生成代码前请逐项检查：

- [ ] 检查项1
- [ ] 检查项2
```

## 输出格式

输出纯 JSON 数组，不要有其他文字：

```json
[
  {
    "skill_name": "{{ dsl_prefix }}-fix-xxx",
    "description": "中文描述...",
    "content": "markdown 正文（含 Quick Checklist）"
  }
]
```
