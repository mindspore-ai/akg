你是一个算子优化领域专家。请根据以下 evolved skill 的名称和描述，按**优化主题**进行聚类分组。

## 待分组的 Skills（共 {{ skill_count }} 个）

{% for s in summaries %}
- **[skill-name]** `{{ s.name }}`: **[描述]** {{ s.description }}
{% endfor %}

## 任务

将上述 skills 按优化主题分组。分组原则：
1. **以描述为准**：重点关注 `description` 字段描述的优化手段和方法论，而非 skill 名称。名称相似但优化手段不同应分到不同组；名称不同（算子类型不同）但优化手段相同应归为同一组。
2. **识别核心优化方向**：判断每个 skill 的核心优化手段是什么（如 BLOCK_SIZE 调优、内存访问优化、流水线并行、向量化等），相同方向的归为一组。
3. **主题粒度适中**：不要过细（每个 skill 单独一组）也不要过粗（全部合为一组）。

## 输出格式

输出 JSON，格式如下：

```json
{
  "clusters": [
    {
      "reason": "为什么这些 skill 归为一组（中文，1-2 句话）",
      "skills": ["上方列出的 skill-name，必须完全一致"]
    }
  ]
}
```

注意：
- `skills` 列表中的名称必须与上方列出的 skill 名称**完全一致**
- 每个 skill 只能出现在一个 cluster 中
- 所有 skill 都必须被分配到某个 cluster
