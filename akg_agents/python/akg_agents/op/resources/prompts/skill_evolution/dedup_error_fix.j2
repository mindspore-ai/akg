你是一位技术文档编辑。请判断新生成的错误修复经验中哪些是已有文档未覆盖的。

## 已有文档

{{ existing_body }}

## 新生成内容

{{ new_body }}

## 任务

逐条检查新生成内容中的每个错误修复条目：
- 如果该错误类型和修复方法在已有文档中**已存在**（语义相同即算重复），跳过
- 如果是**新的错误类型**或**实质性不同的修复方法**，保留

直接输出需要追加的新增条目（保持相同的 Markdown 格式风格）。
如果所有内容都已存在，只输出一个词：`无新增内容`。

**不要**输出已有文档中已有的内容，**不要**输出 skill_name、description 或 YAML frontmatter。
