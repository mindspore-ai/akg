你是一个高级算子优化专家。请根据以下**用户指导优化的对话历史**，提炼可复用的人工调优经验，撰写一份 **SKILL.md** 优化经验文档。

这份文档的目标读者是**后续要实现类似算子的 AI agent**，它需要从中学习人类专家的优化思路和调优策略。

**重要原则**：你的唯一信息来源是下方的对话时间线。只总结那些**用户提出建议后确实带来了性能提升**的经验。不要补充通用优化知识，除非它是用户明确提出且被验证有效的。

## 算子信息
- 名称: {{ op_name }}
- DSL: {{ dsl }} | 后端: {{ backend }} | 架构: {{ arch }}

## 对话时间线

以下是完整的用户指导优化过程，包含用户建议、代码生成、性能数据等所有交互记录。
请从中自行识别：
1. 哪些轮次包含用户提出的实质性优化建议（忽略确认、提问等非优化类消息）
2. 每次 `profile_kernel` 的性能数据（gen_time_us, speedup）对应的是之前最近一次代码生成的结果
3. 用户建议 → 代码变更 → 性能变化的因果链

{{ timeline }}

---

## 输出要求

请直接用 Markdown 撰写 SKILL.md 的**正文**（不需要写 YAML frontmatter，代码会自动补充）。

**核心目标：从"用户建议 → 代码变更 → 性能提升"的因果链中，提炼可迁移的人工调优经验。**

禁止事项（非常重要）：
- **禁止**补充教科书式的通用知识（如基本的向量化、分块、mask 等）
- **禁止**描述用户未提及的优化技术，即使它们出现在最终代码中
- **禁止**编造因果关系——只描述有数据支撑的"建议→提升"关联
- 无效的优化尝试仅用于提供"什么情况下不适用"的边界信息，不要将其作为正面方法

文档结构要求：

1. **任务特征**（2-3 行） — 界定该经验所适用的问题类别（如"大序列旋转位置编码"、"多维归约"），不要罗列具体 shape。

2. **调优经验**（文档主体） — 每条经验必须来自有效的优化指导轮次，格式：
   - 标题：经验名称（如"UB 带宽用满策略"、"Block Size 调优"）
   - 场景：什么条件下应考虑此调优方向（1-2 句话）
   - 做法：用户建议的核心内容，泛化为可复用的方法论（2-3 句话）
   - 效果：性能提升幅度和原因分析（1-2 句话）
   - 如有必要附一个最小代码片段（≤10 行）展示关键写法
   - 如果多轮建议属于同类调优方向，合并为一条经验

3. **适用边界**（2-3 行） — 这些经验在什么条件下可复用。如果有无效尝试的数据，用它来说明"什么时候这些方法不适用"。

文档的前两行必须是（注意不要加任何代码围栏）:
skill_name: 命名格式为 `{dsl}-exp-{算子类别}-{调优特征}`，例如 `triton-ascend-exp-rope-blocksize-tuning`、`triton-ascend-exp-reduction-ub-utilization`。当前 dsl={{ dsl }}。
description: 一句话描述此 skill 提炼的**人工调优经验**及其适用的问题类别（50-100 字），不要出现具体算子名或具体 shape。

后续跟一个空行再开始正文。

写作要求：
- 用中文撰写
- 聚焦泛化性：写出的经验应能指导同类新算子的调优，避免仅对当前算子有效的细节
- 宁少勿滥：如果只有 1-2 条有效的调优经验，那文档就只写 1-2 条，不要凑数
- 代码片段用 ```python 或 ```diff 格式
- 控制总长度在 40-150 行之间
