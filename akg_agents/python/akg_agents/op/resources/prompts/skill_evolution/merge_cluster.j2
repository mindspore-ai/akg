你是一个算子优化文档编辑专家。请将以下多个同主题的优化经验文档合并为一份精简、无重复的文档。

## 分组原因：{{ cluster_reason }}

## 待合并的文档（共 {{ doc_count }} 份）

{% for doc in documents %}
### 文档 {{ loop.index }}: {{ doc.name }}

{{ doc.content }}

---
{% endfor %}

## 合并要求

1. **去重**：相同或语义相同的优化方法只保留一份，选择描述最完整、最具泛化性的版本
2. **泛化**：去除特定算子名称（如"ReLU"、"matmul"），将方法泛化为可迁移的通用经验
3. **合并同类**：如果多份文档中的某些方法属于同一优化方向的不同侧面，合并为一个完整的方法描述
4. **保留代码**：保留有价值的代码片段，但只保留最具代表性的版本
5. **结构统一**：使用统一的文档结构（见下方格式）

## 输出格式

文档开头必须包含以下两行元数据（各占一行）：

skill_name: {{ dsl_prefix }}-merged-{调优特征}
description: 一句话描述合并后 skill 覆盖的优化方法论（50-100 字）

其中 `{调优特征}` 用英文连字符分隔的 2-3 个单词概括核心优化方向（如 memory-access-optimization、block-size-tuning）。

然后空一行，输出正文：

## 适用场景

这些优化方法适用的问题类别（1-2 句话）。

## 优化方法

### 1. 方法一名称

**描述**：什么条件下用、怎么做、为什么有效（1-2 句话）。

```python
# 如有必要附代码片段（≤10 行）
```

### 2. 方法二名称

**描述**：...

## 适用边界

这些方法的限制条件（1-2 句话）。

写作要求：
- 用中文撰写
- 不要输出 YAML frontmatter
- 不要保留特定算子名称，聚焦通用方法论
- 语言简练，控制总长度在 50-120 行
