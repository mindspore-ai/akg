{# KernelGen Agent 的用户 Prompt 模板 #}

## 相关技能和知识

{% if skill_contents %}
{{ skill_contents }}
{% else %}
暂无相关 Skill。
{% endif %}

{% if aggregated_api_docs %}
---

{{ aggregated_api_docs }}
{% endif %}

---
{%- if designer_code %}

## 算法设计草图

以下是设计阶段生成的算法草图/伪代码，请以此为基础进行代码实现，
充分利用其中的算法思路和优化策略：

```
{{ designer_code }}
```

---
{%- endif %}
{%- if previous_code and user_requirements %}
{# ==================== 修改模式 ==================== #}

## ⚠️ 任务模式：代码修改

**你正在修改已有的 kernel 代码，而不是从零生成。**

### 🎯 用户修改需求（最高优先级）

{{ user_requirements }}

### 需要修改的代码

以下是之前生成的 kernel 代码，请根据上述修改需求进行修改：

```python
{{ previous_code }}
```

### 修改原则

1. **仅修改用户要求的部分**，不要重构无关代码
2. **保持代码结构和接口不变**（除非用户要求修改）
3. **确保修改后的代码仍然是完整可运行的**
4. 输出完整的修改后代码（包含所有 import 和 class 定义）

---
{%- endif %}

{% if verifier_error or conductor_suggestion or code_check_errors %}
## 执行历史和反馈

{% if code_check_errors %}
### 代码静态检查错误（需优先修复）

**以下是代码静态检查发现的问题，这些是语法/编译/导入级别的错误，必须修复：**

{{ code_check_errors }}

**注意：上述问题是基于静态检查发现的确定性错误，请严格按照建议修改。**
{% endif %}

{% if verifier_error %}

### Verifier 错误信息

上一次代码验证失败，请根据以下错误信息修复代码：

```
{{ verifier_error }}
```
{% endif %}
{% if conductor_suggestion %}

### Conductor 修复建议

{{ conductor_suggestion }}
{% endif %}

---
{%- endif %}
{%- if history_actions %}

## 执行历史和上下文
{% for action in history_actions %}
{% if action.tool_name == "history_summary" %}
### 历史总结
{{ action.result.get("summary", "无总结") }}

{% else %}
### 步骤 {{ loop.index }}: {{ action.tool_name }}

**输入参数**:
{% for key, value in action.arguments.items() %}
- `{{ key }}`: {{ value | string | truncate(100) }}
{% endfor %}

**输出结果**:
{% if action.result.get("_is_summary") %}
{{ action.result.get("summary", "无结果") }}
{% else %}
{% for key, value in action.result.items() %}
- `{{ key }}`: {{ value | string | truncate(100) }}
{% endfor %}
{% endif %}

{% endif %}
{% endfor %}
---
{%- endif %}

## 当前任务

**算子名称**: `{{ op_name }}`  
**函数名称**: `{{ func_name }}`  

### 任务描述（框架参考代码）

{{ task_desc }}

{% if bench_type == "sol" %}
**⚠️ 重要（SOL-ExecBench 模式）**：验证框架通过 `ModelNew()` 实例化（**无参数**），然后按 `definition.json` 中 `inputs` 的声明顺序生成输入列表，以 `model(*inputs_list)` 调用 `forward`。因此：
- `__init__(self)` **不接收额外参数**
- `forward(self, ...)` 的参数必须与 `definition.json` 的 `inputs` 字段**按顺序一一对应**（包括标量参数如 `float`）
- 返回值必须与 `reference.py` 中 `run()` 函数的返回值一致（数量、dtype、shape）
{% else %}
**⚠️ 重要**：验证框架通过 `ModelNew(*get_init_inputs())` 实例化、`model(*get_inputs())` 调用，因此 `__init__` 和 `forward` 的参数必须与上述函数返回值一一对应。
{% endif %}

---
{%- if user_requirements and not previous_code %}
{# 首次生成但有额外需求 #}

### ⚠️ 用户额外需求（请务必遵守）

{{ user_requirements }}

---
{%- endif %}
{%- if inspirations %}

## 进化探索方案

**重要须知**：下面是一些过往方案的代码以及性能指标，请参考这些信息，分析可用的优化方案，并生成优化后的代码。

{{ inspirations }}

一般而言，调整切分方式、修改计算逻辑、调整计算顺序、调整计算精度等，都是可行的优化方案。
请你尽最大努力来完成代码的优化，得到一个计算速度快的高效算子实现。

---
{%- endif %}
{%- if handwrite_suggestions %}

## 手写优化策略与实现参考

以下是针对类似算子的专家级优化策略和实现，请深入理解这些优化思路并应用到草图设计中：

{% for suggestion in handwrite_suggestions %}
### 参考案例 {{ loop.index }}: {{ suggestion.name }}

**优化策略与要点：**
```markdown
{{ suggestion.improvement_doc }}
```

---
{% endfor %}
{%- endif %}
{%- if previous_code and not user_requirements %}
{# 有之前的代码但没有新需求（例如 verifier 迭代修复）#}

### 上一版代码（参考）

以下是之前生成的代码，可作为参考进行改进：

```python
{{ previous_code }}
```

---
{%- endif %}
{%- if hardware_docs %}

## 目标硬件信息

**架构**: {{ arch_name or '通用' }}

{{ hardware_docs }}

---
{%- endif %}

## 生成指令

{% if previous_code and user_requirements %}
根据用户的修改需求，修改上述已有代码。输出**完整的修改后代码**。
{% else %}
根据以上信息生成完整的内核代码。
{% endif %}

**代码要求**:
- 语法正确
- 针对目标硬件优化
- 正确处理边界条件
- 包含必要的导入和包装函数
- 代码结构清晰，适当添加注释说明关键逻辑

## 代码生成格式规范

**重要：所有生成的代码必须是ModelNew类格式，不再使用函数形式。**

1. 必须生成ModelNew类，格式如下：
```python
{% if dsl == "torch" %}
import torch
import torch.nn as nn
import torch.nn.functional as F

# 注意：禁止使用自定义 Kernel（如 triton、cuda_extension）！这是 Kernel → PyTorch 转换任务

class ModelNew(nn.Module):
    """
    PyTorch 原生实现（从自定义 Kernel 转换）
    """
    def __init__(self, ...):
        super().__init__()
        # 如果有可学习参数（如 Linear, Conv2d），在此定义
        # 需要设置固定随机种子以确保权重一致
        torch.manual_seed(0)
    
    def forward(self, ...):
        # 使用 PyTorch 原生操作实现等价功能
        # 常见转换示例：
        # Triton: tl.maximum(x, 0) → torch.relu(x)
        # Triton: tl.sum(x, axis=0) → torch.sum(x, dim=0)
        # CUDA: max(a, b) → torch.maximum(a, b)
        # CUDA: __syncthreads() → (PyTorch 自动同步，无需显式调用)
        return output
{% elif "triton_cuda" in dsl or "triton_ascend" in dsl %}
import torch
import triton
import triton.language as tl

@triton.jit
def {{ op_name }}_kernel(...):
    # kernel实现
    ...

class ModelNew(torch.nn.Module):
    def __init__(self, ...):
        super().__init__()
        # 如果有nn.Module参数（如nn.Linear, nn.Conv2d），需要：
        # 1. 在__init__开始时设置固定随机种子（与验证模板中的种子一致）
        # 2. 通过nn.Linear/nn.Conv2d等构建参数，然后提取weight和bias
        # 3. 使用nn.Parameter包装，确保与原始Model的权重一致
        torch.manual_seed(0)  # 固定种子（与kernel_verify_template.j2中的种子一致）
        # 例如：
        # linear = nn.Linear(in_features, out_features)
        # self.weight = nn.Parameter(linear.weight.clone())
        # self.bias = nn.Parameter(linear.bias.clone()) if linear.bias is not None else None
    
    def forward(self, ...):
        # 调用kernel函数
        return {{ op_name }}_kernel(...)
{% elif dsl == "cpp" %}
import torch
import torch.nn as nn
from torch.utils.cpp_extension import load_inline

# C++ kernel代码
cpp_source = """
...
"""

# 加载C++扩展
xxx_module = load_inline(...)

class ModelNew(nn.Module):
    def __init__(self, ...):
        super().__init__()
        # 如果有nn.Module参数，需要设置随机种子并提取参数
        torch.manual_seed(0)  # 固定种子
        # 例如：
        # linear = nn.Linear(in_features, out_features)
        # self.weight = nn.Parameter(linear.weight.clone())
        # self.bias = nn.Parameter(linear.bias.clone()) if linear.bias is not None else None
    
    def forward(self, ...):
        # 调用C++ kernel
        return xxx_module.xxx_kernel(...)
{% elif framework == "mindspore" %}
import mindspore as ms
from mindspore import nn
{% if "triton_cuda" in dsl or "triton_ascend" in dsl %}
import triton
import triton.language as tl

@triton.jit
def {{ op_name }}_kernel(...):
    # kernel实现
    ...

class ModelNew(nn.Cell):
    def __init__(self, ...):
        super().__init__()
        # 如果有nn.Cell参数（如nn.Dense, nn.Conv2d），需要：
        # 1. 在__init__开始时设置固定随机种子（与验证模板中的种子一致）
        # 2. 通过nn.Dense/nn.Conv2d等构建参数，然后提取weight和bias
        # 3. 使用Parameter包装，确保与原始Model的权重一致
        ms.set_seed(0)  # 固定种子（与kernel_verify_template.j2中的种子一致）
        # 例如：
        # dense = nn.Dense(in_features, out_features)
        # self.weight = Parameter(dense.weight.clone(), name="weight")
        # self.bias = Parameter(dense.bias.clone(), name="bias") if dense.bias is not None else None
    
    def construct(self, ...):
        # 调用kernel函数
        return {{ op_name }}_kernel(...)
{% else %}
# 其他DSL类似，必须生成ModelNew类
{% endif %}
```
{% endif %}
**注意**：以上self属性只针对于需要用到内置参数的实现，对于使用get_inputs或get_init_inputs传入的参数，需要直接调用。

2. 对于无参数的算子（如ReLU），`__init__`可以为空：
```python
{% if framework == "torch" %}
class ModelNew(nn.Module):
    def __init__(self):
        super().__init__()
    
    def forward(self, x):
        return xxx_kernel(x)
{% elif framework == "mindspore" %}
class ModelNew(nn.Cell):
    def __init__(self):
        super().__init__()
    
    def construct(self, x):
        return xxx_kernel(x)
{% endif %}
```

3. 对于有参数的算子（如Linear/Dense, Conv2d），必须在`__init__`中通过固定随机种子构建参数：
```python
{% if framework == "torch" %}
class ModelNew(nn.Module):
    def __init__(self, in_features, out_features):
        super().__init__()
        torch.manual_seed(0)  # 固定种子，确保与原始Model权重一致
        linear = nn.Linear(in_features, out_features)
        self.weight = nn.Parameter(linear.weight.clone())
        self.bias = nn.Parameter(linear.bias.clone()) if linear.bias is not None else None
    
    def forward(self, x):
        return xxx_kernel(x, self.weight, self.bias)
{% elif framework == "mindspore" %}
class ModelNew(nn.Cell):
    def __init__(self, in_features, out_features):
        super().__init__()
        ms.set_seed(0)  # 固定种子，确保与原始Model权重一致
        from mindspore import Parameter
        dense = nn.Dense(in_features, out_features)
        self.weight = Parameter(dense.weight.clone(), name="weight")
        self.bias = Parameter(dense.bias.clone(), name="bias") if dense.bias is not None else None
    
    def construct(self, x):
        return xxx_kernel(x, self.weight, self.bias)
{% endif %}
```
**注意**：以上self属性只针对于需要用到内置参数的实现，对于使用get_inputs或get_init_inputs传入的参数，需要直接调用。

{% if bench_type == "sol" %}
4. **SOL-ExecBench 模式的接口规范**（验证框架会按此方式调用你的代码）：

验证框架按以下方式实例化和调用 `ModelNew`：
```python
init_params = []                           # 固定为空列表
impl_model = ModelNew(*init_params)        # 即 ModelNew()
impl_model = impl_model.to(device)

# 按 definition.json 的 inputs 顺序生成输入列表
inputs_list = gen_inputs(definition, workload, device, ...)
ref_outputs = reference.run(*inputs_list)  # 参考实现
impl_outputs = impl_model(*inputs_list)   # 你的实现
```

因此：
- **`__init__(self)` 不接收额外参数**。如果需要可学习参数（如权重），在 `__init__` 中通过固定随机种子创建。
- **`forward(self, ...)` 的参数必须与 `definition.json` 的 `inputs` 字段按声明顺序一一对应**。例如 `inputs` 依次声明了 `grad_output`、`reshaped`、`weight`，则必须写 `forward(self, grad_output, reshaped, weight)`。
- **注意标量参数**：`inputs` 中可能包含标量（如 `float` 类型的 `attention_dropout`），它们不是 tensor，不要对其调用 `.shape` 等 tensor 方法。
- **返回值必须与 `reference.py` 中 `run()` 函数的返回值一致**（数量、dtype、shape）。
- **所有可变维度从输入 tensor 的 `.shape` 获取**，不要硬编码可变维度（如 batch_size、seq_len）；常量维度（definition 中 `type: const`）可以硬编码。
{% else %}
4. 对于任务输入中固定写死的init_inputs参数，以及class外定义的参数，硬编码至代码中。
所需要的shape参数，要从inputs的数据形状中获取，以适应不同的输入(**重要** 需要仔细检查输入的变量和shape与代码中一一对应）：
```python
{% if framework == "torch" %}
def forward(self, input_tensor, ...):
    # 硬编码的参数（如果有无法从inputs中获取的参数）
    # args = ... # 无法从inputs中获取的参数信息硬编码于此
    
  # 从输入张量获取shape参数
  P1, P2, P3 = input_tensor.shape  # 变量名应该与inputs构造时对应的变量保持一致
  ...
  # 执行 kernel 函数
  ...
{% elif framework == "mindspore" %}
def construct(self, input_tensor, ...):
    # 硬编码的参数（如果有无法从inputs中获取的参数）
    # args = ... # 无法从inputs中获取的参数信息硬编码于此
    
  # 从输入张量获取shape参数
  P1, P2, P3 = input_tensor.shape  # 变量名应该与inputs构造时对应的变量保持一致
  ...
  # 执行 kernel 函数
  ...
{% endif %}
```
- **`__init__` 的参数必须与 `get_init_inputs()` 返回值一一对应**。如果返回空列表则 `__init__(self)` 无额外参数；如果返回 `[num_features, num_groups]`，则必须写 `__init__(self, num_features, num_groups)`。
- **`forward` 的参数必须与 `get_inputs()` 返回值一一对应**。
{% endif %}

{% if "triton_cuda" in dsl or "triton_ascend" in dsl %}
5. 卷积类算子生成注意事项：
请注意！！如果检测到给出的任务是卷积类的算子任务，为了保证ModelNew的卷积核权重与当前任务代码的卷积核权重一致，需要在ModelNew的`__init__`中通过固定随机种子构建参数：
```python
{% if framework == "torch" %}
class ModelNew(torch.nn.Module):
    def __init__(self, in_channels, out_channels, kernel_size, ...):
        super().__init__()
        torch.manual_seed(0)  # 固定种子，确保与原始Model权重一致
        # 创建Conv层并提取weight和bias
        conv = nn.Conv2d(in_channels, out_channels, kernel_size, ...)
        self.weight = nn.Parameter(conv.weight.clone())
        self.bias = nn.Parameter(conv.bias.clone()) if conv.bias is not None else None
    
    def forward(self, x):
        return conv_kernel(x, self.weight, self.bias, ...)
{% elif framework == "mindspore" %}
class ModelNew(nn.Cell):
    def __init__(self, in_channels, out_channels, kernel_size, ...):
        super().__init__()
        ms.set_seed(0)  # 固定种子，确保与原始Model权重一致
        from mindspore import Parameter
        # 创建Conv层并提取weight和bias
        conv = nn.Conv2d(in_channels, out_channels, kernel_size, ...)
        self.weight = Parameter(conv.weight.clone(), name="weight")
        self.bias = Parameter(conv.bias.clone(), name="bias") if conv.bias is not None else None
    
    def construct(self, x):
        return conv_kernel(x, self.weight, self.bias, ...)
{% endif %}
```
请务必保证nn中调用的module要与任务代码中调用的module要一致，使用的参数要一致，device设置请参考不同的硬件后端（"cuda", "npu", "ascend"）。
{% endif %}
