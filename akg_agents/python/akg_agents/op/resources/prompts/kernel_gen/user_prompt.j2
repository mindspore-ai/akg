{# KernelGen Agent 的用户 Prompt 模板 #}

## 相关技能和知识

{% if skill_contents %}
{{ skill_contents }}
{% else %}
暂无相关 Skill。
{% endif %}

---

{% if designer_code %}
## 算法设计草图

以下是设计阶段生成的算法草图/伪代码，请以此为基础进行代码实现，
充分利用其中的算法思路和优化策略：

```
{{ designer_code }}
```

---

{% endif %}


{% if previous_code and user_requirements %}
{# ==================== 修改模式 ==================== #}

## ⚠️ 任务模式：代码修改

**你正在修改已有的 kernel 代码，而不是从零生成。**

### 🎯 用户修改需求（最高优先级）

{{ user_requirements }}

### 需要修改的代码

以下是之前生成的 kernel 代码，请根据上述修改需求进行修改：

```python
{{ previous_code }}
```

### 修改原则

1. **仅修改用户要求的部分**，不要重构无关代码
2. **保持代码结构和接口不变**（除非用户要求修改）
3. **确保修改后的代码仍然是完整可运行的**
4. 输出完整的修改后代码（包含所有 import 和 class 定义）

---

{% endif %}

{% if verifier_error or conductor_suggestion %}
## 执行历史和反馈

{% if verifier_error %}
### Verifier 错误信息

上一次代码验证失败，请根据以下错误信息修复代码：

```
{{ verifier_error }}
```

{% endif %}

{% if conductor_suggestion %}
### Conductor 修复建议

{{ conductor_suggestion }}

{% endif %}

---

{% endif %}

{% if history_actions %}
## 执行历史和上下文
{% for action in history_actions %}
{% if action.tool_name == "history_summary" %}
### 历史总结
{{ action.result.get("summary", "无总结") }}

{% else %}
### 步骤 {{ loop.index }}: {{ action.tool_name }}

**输入参数**:
{% for key, value in action.arguments.items() %}
- `{{ key }}`: {{ value | string | truncate(100) }}
{% endfor %}

**输出结果**:
{% if action.result.get("_is_summary") %}
{{ action.result.get("summary", "无结果") }}
{% else %}
{% for key, value in action.result.items() %}
- `{{ key }}`: {{ value | string | truncate(100) }}
{% endfor %}
{% endif %}

{% endif %}
{% endfor %}
{% endif %}

---

## 当前任务

**算子名称**: `{{ op_name }}`  
**函数名称**: `{{ func_name }}`  

### 任务描述（框架参考代码）

{{ task_desc }}

---

{% if user_requirements and not previous_code %}
{# 首次生成但有额外需求 #}
### ⚠️ 用户额外需求（请务必遵守）

{{ user_requirements }}

---

{% endif %}


{% if inspirations %}
## 进化探索方案

**重要须知**：下面是一些过往方案的代码以及性能指标，请参考这些信息，分析可用的优化方案，并生成优化后的代码。

{{ inspirations }}

一般而言，调整切分方式、修改计算逻辑、调整计算顺序、调整计算精度等，都是可行的优化方案。
请你尽最大努力来完成代码的优化，得到一个计算速度快的高效算子实现。

---

{% endif %}

{% if handwrite_suggestions %}
## 手写优化策略与实现参考

以下是针对类似算子的专家级优化策略和实现，请深入理解这些优化思路并应用到草图设计中：

{% for suggestion in handwrite_suggestions %}
### 参考案例 {{ loop.index }}: {{ suggestion.name }}

**优化策略与要点：**
```markdown
{{ suggestion.improvement_doc }}
```

---

{% endfor %}
{% endif %}

{% if previous_code and not user_requirements %}
{# 有之前的代码但没有新需求（例如 verifier 迭代修复）#}
### 上一版代码（参考）

以下是之前生成的代码，可作为参考进行改进：

```python
{{ previous_code }}
```

---

{% endif %}

{% if hardware_docs %}
## 目标硬件信息

**架构**: {{ arch_name or '通用' }}

{{ hardware_docs }}

---

{% endif %}

## 生成指令

{% if previous_code and user_requirements %}
根据用户的修改需求，修改上述已有代码。输出**完整的修改后代码**。
{% else %}
根据以上信息生成完整的内核代码。
{% endif %}

**代码要求**:
- 语法正确
- 针对目标硬件优化
- 正确处理边界条件
- 包含必要的导入和包装函数
- 代码结构清晰，适当添加注释说明关键逻辑

