你是一个 {{ dsl }} 代码检查专家。请分析以下代码是否存在语法或 API 使用问题。

## 目标 DSL 和后端
- DSL: {{ dsl }}
- Backend: {{ backend }}

## 待检查代码
```python
{{ code }}
```

{% if basic_docs %}
## {{ dsl }} 编程指南
{{ basic_docs }}

请仔细阅读上述编程指南，了解 {{ dsl }} 的语法规范、API 使用方式和限制条件。
{% endif %}

{% if suggestion_docs %}
## {{ dsl }} 编程建议和限制
{{ suggestion_docs }}

请仔细阅读上述编程建议，了解 {{ dsl }} 的常见错误、禁止使用的语法和推荐的最佳实践。
{% endif %}

{% if expert_suggestion %}
## 额外编程规范参考
{{ expert_suggestion }}
{% endif %}

## 检查任务

请根据上述文档（编程指南和编程建议），仔细检查待检查代码是否存在以下问题：

1. **语法错误**：代码是否违反了 {{ dsl }} 的语法规范？
2. **API 使用错误**：是否使用了不存在的 API 或错误地使用了 API？
3. **禁止语法**：是否使用了文档中明确禁止的语法（如 break、continue、return、while 等）？
4. **限制条件违反**：是否违反了文档中提到的限制条件（如 BLOCK_SIZE 限制、Grid 限制等）？
5. **最佳实践违反**：是否违反了文档中推荐的最佳实践？

对于发现的每个问题，请明确指出：
- **问题所在的行号**
- **具体的问题描述**（说明违反了文档中的哪一条规则或建议）
- **修复建议**（参考文档中的推荐做法）

## 输出格式

请以 JSON 格式返回检查结果：

```json
{
    "passed": true或false,
    "errors": [
        {
            "line": 行号,
            "detail": "问题描述",
            "suggestion": "修复建议",
            "code_snippet": "问题代码片段"
        }
    ]
}
```

如果代码没有问题，返回：
```json
{
    "passed": true,
    "errors": []
}
```

请开始检查：

