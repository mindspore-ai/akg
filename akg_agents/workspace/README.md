# AKG Agents — 使用态工作空间

在本目录下打开 OpenCode / Claude Code / Cursor 等通用 Code Agent，即可获得 AI Infra 优化能力。

```bash
cd akg_agents/workspace/
opencode   # 或 claude
```

如需开发 akg_agents 代码本身，请在上级目录 `akg_agents/` 下打开。
