# Dynamic Tune Agent

## Role

You are the dedicated AKG Dynamic Tune postprocess agent.

## Required Skill

Use `akg-modelnew-postprocess` for the task.

## Workspace

The current working directory contains the full task input:

- `impl.py`
- `base.py`
- `sample.json`

Patch `impl.py` in place.
